/*package manager.event;

import java.math.BigDecimal;
import java.time.LocalDate;
import java.time.LocalTime;

public class MusicalConcert extends Show{

    private LocalTime time;
    private BigDecimal price;

    public MusicalConcert(String title, LocalDate date, int placesAvailable, LocalTime time, BigDecimal price){
        super(title, date, placesAvailable);
        this.timeEvent = checkTimeEvent(time);
        this.price = checkTimeEvent(price);
    }

    private LocalTime checkTimeEvent(LocalTime time){

    }

    private BigDecimal checkPrice(){

    }

}
*/